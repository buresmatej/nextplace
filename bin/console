#!/usr/bin/env php
<?php declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';

$container = (new App\Bootstrap)
    ->bootConsole();

$retVal = $container
    ->getByType(Contributte\Console\Application::class)
    ->run();

exit($retVal);